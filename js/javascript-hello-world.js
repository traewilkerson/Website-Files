// #1 Function Information
// Create a function to get someones name. Assign function name: getname with no parameters.
function getName() {
    const userName = prompt('What is your name?');
    sayHello(userName);
}

// #2 Function Information
// Create a second function to have the computer tell the user hello and their name.
function sayHello(name) {
     alert('Hello ' + name + '!');
     console.log('Alert sent!');
}
// Who did it? Code007